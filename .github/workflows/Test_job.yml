name: demo test CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build job
        run: echo "this is a demo test job"

  test:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: test job
        run: echo "this is a demo test job"
